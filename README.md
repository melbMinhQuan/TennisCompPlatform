# TennisComp

Minimal Vite + React frontend and NestJS backend.

```bash
npm install
npm run dev:backend   # http://localhost:3000
npm run dev:frontend  # http://localhost:5173
```

Try `john` (admin), `johnny` (player), or any other name (none).
